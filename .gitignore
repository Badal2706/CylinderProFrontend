# Secrets
.env
.env.*
!.env.example

# Dependencies — regenerated by `npm ci` on whatever machine runs it.
# Never commit: contains platform-specific binaries (esbuild/rollup native
# builds differ per OS+arch), which corrupt checkouts across machines.
node_modules/

# Build output — regenerated by `npm run build`
dist/

# OS / editor cruft
.DS_Store
Thumbs.db
